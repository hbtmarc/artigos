---
import { withBase } from '../lib/withBase';

const homeHref = withBase('');
const postsHref = withBase('posts/artigo-exemplo/');
const adminHref = withBase('admin/');
---
<header class="site-header">
  <div class="brand">
    <a class="brand-title" href={homeHref}>Artigos</a>
    <p class="brand-tagline">Notas longas sobre produto, design e tecnologia.</p>
  </div>
  <nav class="site-nav" aria-label="Principal">
    <a href={homeHref}>Home</a>
    <a href={postsHref}>Posts</a>
    <a href={adminHref}>Admin</a>
  </nav>
</header>
